{
  "name": "forever-armory",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "dev": "node dev-server.mjs",
    "test:security": "firebase emulators:exec --project demo-forever-armory --only firestore \"node --test tests/security/*.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.19.0",
    "firebase-tools": "^14.22.0"
  }
}
